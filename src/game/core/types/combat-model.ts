import type {
  RaidCombatCard,
  RaidEnemyCard,
  RaidLayerState,
  RaidOutcome,
  RaidState
} from "../../raid/types";

export type CombatCardModel = RaidCombatCard;
export type EnemyCombatCardModel = RaidEnemyCard;
export type CombatLayerModel = RaidLayerState;
export type CombatOutcomeModel = RaidOutcome;
export type CombatStateModel = RaidState;

